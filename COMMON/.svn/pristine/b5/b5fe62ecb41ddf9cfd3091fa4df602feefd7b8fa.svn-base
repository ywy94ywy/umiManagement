/**
 * title: 模态框按钮
 * desc: 糅合模态框与按钮，状态属性内置
 */
import React from 'react';
import { ButtonModal } from 'lanlinker';

export default () => {
  return (
    <ButtonModal
      buttonProps={{
        text: '模态框按钮',
        type: 'primary',
      }}
      title="标题"
      onOk={(e, close) => {
        close();
      }}
    >
      内容
    </ButtonModal>
  );
};
